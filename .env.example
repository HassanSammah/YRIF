# Compose interpolation for docker-compose.prod.yml (lives at /var/www/yrif/.env on the VPS).
# CD writes this file from GitHub production secrets. Do not commit real values.

DB_NAME=yrif_db
DB_USER=yrif_user
DB_PASSWORD=change-me
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_GOOGLE_CLIENT_ID=
